import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 2,
    borderRadius: 50,
    margin: 8,
    padding: 8,
  },
  title: {
    fontSize:16,
    color: '#fff',
    textAlign: 'center',
  },
});
